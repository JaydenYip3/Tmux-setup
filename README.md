Setup:
Download tmux with your respective package manager

`brew install tmux`

To enter Tmux workspace type `tmux`
To change configs find your `.tmux.conf` file
Usually it should be under `~/.tmux.conf`

To copy and paste you will have to use `cntrl` instead of cmd on mac
